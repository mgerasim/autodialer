<% provide(:title, @task.name) %>
<%= link_to 'Назад', tasks_path, class: 'btn btn-primary' %>
<p>
  <strong>Наименование:</strong>
  <%= @task.name %>
</p>


<table class="table table-hover">
  <thead>
    <tr>
      <th>#</th>
      <th>Номер</th>
      <th>Пир</th>
      <th>Статус</th>
    </tr>
  </thead>
  <tbody>
    
    <% @task.contacts.each do |contact| %>
      <tr>
         <td><%= contact.id %></td>
         <td><%= contact.phone %></td>
         <td><%= contact.peer %></td>
         <td><%= contact.status %></td>
      </tr>
   <% end %>

  </tbody>
</table>

<%= link_to 'Назад', tasks_path, class: 'btn btn-primary' %>
